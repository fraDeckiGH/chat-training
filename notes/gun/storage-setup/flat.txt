

the one that goes `${prop}:${el[prop]}/id:${el.id}`
ie `date:<dateValue>/id:<idValue>`

by default the above is done for each field in an entity
the id at the end it's there in case of duplicate field values

the nodes created store a gun-node-reference to the entity
they belong to








